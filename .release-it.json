{
  "increment": "conventional:angular",
  "scripts": {
    "beforeStart": "yarn run lint && yarn run build && yarn run test --colors && yarn run test:cypress && yarn run test:size"
  },
  "git": {
    "requireUpstream": false,
    "requireCleanWorkingDir": false,
    "commitMessage": "chore(release): %s"
  }
}
