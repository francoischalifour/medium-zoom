{
  "requireUpstream": false,
  "increment": "conventional:angular",
  "src": {
    "commitMessage": "chore(release): %s",
    "beforeStartCommand": "yarn run lint && yarn run test -- --colors && yarn run test:e2e -- --colors && yarn run build && yarn run test:size"
  }
}
